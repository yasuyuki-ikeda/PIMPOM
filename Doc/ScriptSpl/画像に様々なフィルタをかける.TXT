//PIMPOMコマンドプロンプト　サンプルプログラム
//画像データ0に対して様々なフィルタを実行する

//移動平均をフィルタサイズ5で実行して画像メモリ10に入れる
FilterSmooth(0,10,5,5,0,1)

//移動平均をフィルタサイズ11で実行して画像メモリ11に入れる
FilterSmooth(0,11,11,11,0,1)

//ガウスフィルタをフィルタサイズ11で実行して画像メモリ12に入れる
FilterGauss(0,12,11,0,1,1)

//メディアンフィルタをフィルタサイズ3で実行して画像メモリ13に入れる
FilterMedian(0,13, 3, 3, 0,1)

//オープニングフィルタをフィルタサイズ3で実行して画像メモリ14に入れる
FilterOpening(0,14, 3, 3, 0,1)

//クロージングフィルタをフィルタサイズ3で実行して画像メモリ15に入れる
FilterClosing(0,15, 3, 3, 0,1)

//ラプラシアンオブガウシアン(LoG)フィルタをフィルタサイズ11で実行して画像メモリ16に入れる
FilterLaplaceGauss(0,16,11,0,100,0.1,1,1)

//ソーベルエッジ抽出をフィルタサイズ3実行でして画像メモリ17に入れる
FilterEdgeSobel(0, 17, 3, 100,0,1)

//LoGゼロクロスエッジ抽出をフィルタサイズ11実行でして画像メモリ18に入れる
FilterEdgeLog(0, 18, 11, 10,0,1,1)

//Cannyエッジ抽出をフィルタサイズ3実行でして画像メモリ19に入れる
FilterEdgeCanny(0, 19, 3, 100,50,1)